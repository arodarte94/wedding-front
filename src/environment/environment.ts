const dev = "http://nallelytumblus.wedding/api/";
const prod = window.location.origin + "/api/";
const imagePath = "http://client.pos.core/data/client/";

export const ENV = {
  production: false,
  basePath: dev,
  imagePath: imagePath,
};
